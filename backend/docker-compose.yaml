version: '3.1'
services:
  db:
    image: postgres
    restart: always
    ports:
      - "5433:5433"
    environment:
      POSTGRES_DB: zebrands
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    command: -p 5433
